<!DOCTYPE html>
<html lang="en">

    <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="apple-touch-icon" sizes="76x76" href="../assets/img/apple-icon.png">
    <link rel="icon" type="image/png" href="../assets/img/favicon.png">
    <title>
        PhazeDash
    </title>
    <!--     Fonts and icons     -->
    <link href="https://fonts.googleapis.com/css?family=Poppins:200,300,400,600,700,800" rel="stylesheet" />
    <link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">
    <!-- Nucleo Icons -->
    <link href="../assets/css/nucleo-icons.css" rel="stylesheet" />
    <!-- CSS Files -->
    <link href="../assets/css/black-dashboard.css?v=1.0.0" rel="stylesheet" />
    <!-- CSS Just for demo purpose, don't include it in your project -->
    <link href="../assets/demo/demo.css" rel="stylesheet" />

    <script>
        let editOpen = false
        let editIndex = 0
    </script>
    </head>

    <body <% if(dark === "1"){ %>class="white-content"<% } %>>
    <div class="wrapper">
        <% if(theme === "pink"){ %>
            <div class="sidebar" data="red">
        <% } else if(theme === "blue"){ %>
            <div class="sidebar" data="blue">
        <% } else if(theme === "orange"){ %>
            <div class="sidebar" data="orange">
        <% } else if(theme === "green"){ %>
            <div class="sidebar" data="green">
        <% } else{ %>
            <div class="sidebar">
        <% } %>
        <div class="sidebar-wrapper">
            <div class="logo">
                <a href="javascript:void(0)" class="simple-text logo-mini">
                    PD
                </a>
                <a href="javascript:void(0)" class="simple-text logo-normal">
                    <%= domain.name %>
                </a>
                </div>
                <ul class="nav">
                <li>
                    <a href="/">
                    <i class="tim-icons icon-chart-pie-36"></i>
                    <p>Dashboard</p>
                    </a>
                </li>
                <li>
                    <a href="/logs">
                    <i class="tim-icons icon-align-left-2"></i>
                    <p>Logs</p>
                    </a>
                </li>
                <li>
                    <a href="/management">
                    <i class="tim-icons icon-pin"></i>
                    <p>IP Management</p>
                    </a>
                </li>
                <li class="active ">
                    <a href="/dmanagement">
                    <i class="tim-icons icon-pin"></i>
                    <p>Domain Management</p>
                    </a>
                </li>
                <li>
                    <a href="/alerts">
                    <i class="tim-icons icon-bell-55"></i>
                    <p>Notifications</p>
                    </a>
                </li>
                <li>
                    <a href="/user">
                    <i class="tim-icons icon-single-02"></i>
                    <p>User Profile</p>
                    </a>
                </li>
                <li>
                    <a href="/plugins">
                    <i class="tim-icons icon-puzzle-10"></i>
                    <p>Plugins</p>
                    </a>
                </li>
                </ul>
        </div>
        </div>
        <% if(theme === "pink"){ %>
            <div class="main-panel" data="red">
        <% } else if(theme === "blue"){ %>
            <div class="main-panel" data="blue">
        <% } else if(theme === "orange"){ %>
            <div class="main-panel" data="orange">
        <% } else if(theme === "green"){ %>
            <div class="main-panel" data="green">
        <% } else { %>
            <div class="main-panel">
        <% } %>
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg navbar-absolute navbar-transparent">
            <div class="container-fluid">
            <div class="navbar-wrapper">
                <div class="navbar-toggle d-inline">
                <button type="button" class="navbar-toggler">
                    <span class="navbar-toggler-bar bar1"></span>
                    <span class="navbar-toggler-bar bar2"></span>
                    <span class="navbar-toggler-bar bar3"></span>
                </button>
                </div>
                <a class="navbar-brand" href="javascript:void(0)">Dashboard</a>
            </div>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-bar navbar-kebab"></span>
                <span class="navbar-toggler-bar navbar-kebab"></span>
                <span class="navbar-toggler-bar navbar-kebab"></span>
            </button>
            <div class="collapse navbar-collapse" id="navigation">
                <ul class="navbar-nav ml-auto">
                <li class="search-bar input-group">
                    <button class="btn btn-link" id="search-button" data-toggle="modal" data-target="#searchModal"><i class="tim-icons icon-zoom-split" ></i>
                    <span class="d-lg-none d-md-block">Search</span>
                    </button>
                </li>
                <li class="dropdown nav-item">
                    <a href="javascript:void(0)" class="dropdown-toggle nav-link" data-toggle="dropdown">
                        <div class="notification d-none d-lg-block d-xl-block" id="alertDot"></div>
                        <i class="tim-icons icon-sound-wave"></i>
                        <p class="d-lg-none">
                            Notifications
                        </p>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-right dropdown-navbar" id="notifications">
                        <li class="nav-link"><a href="#" class="nav-item dropdown-item">Mike John responded to your email</a></li>
                        <li class="nav-link"><a href="javascript:void(0)" class="nav-item dropdown-item">You have 5 more tasks</a></li>
                        <li class="nav-link"><a href="javascript:void(0)" class="nav-item dropdown-item">Your friend Michael is in town</a></li>
                        <li class="nav-link"><a href="javascript:void(0)" class="nav-item dropdown-item">Another notification</a></li>
                        <li class="nav-link"><a href="javascript:void(0)" class="nav-item dropdown-item">Another one</a></li>
                        </ul>
                </li>
                <li class="dropdown nav-item">
                    <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
                    <div class="photo">
                        <img src="<%= data.avatar %>" alt="Profile Photo">
                    </div>
                    <b class="caret d-none d-lg-block d-xl-block"></b>
                    <p class="d-lg-none">
                        Log out
                    </p>
                    </a>
                    <ul class="dropdown-menu dropdown-navbar">
                    <li class="nav-link"><a href="./user" class="nav-item dropdown-item">Profile</a></li>
                    <li class="nav-link"><a href="javascript:void(0)" class="nav-item dropdown-item">Settings</a></li>
                    <li class="dropdown-divider"></li>
                    <li class="nav-link"><a href="./login" class="nav-item dropdown-item">Log out</a></li>
                    </ul>
                </li>
                <li class="separator d-lg-none"></li>
                </ul>
            </div>
            </div>
        </nav>
        <div class="modal modal-search fade" id="searchModal" tabindex="-1" role="dialog" aria-labelledby="searchModal" aria-hidden="true">
            <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                <input type="text" class="form-control" id="inlineFormInputGroup" placeholder="SEARCH">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <i class="tim-icons icon-simple-remove"></i>
                </button>
                </div>
            </div>
            </div>
        </div>
        <!-- End Navbar -->
        <div class="content">
            <div class="row">
            <div class="col-md-12">
                <div class="card ">
                <div class="card-header">
                    <h4 class="card-title"> DNS</h4>

                    <button class="btn btn-fill btn-primary" onclick="openDnsAdder(); editOpen = false">Add DNS Record</button>

                    <div id="dnsBoxThingy">
                        <div class="row">
                            <div class="col-md-3 px-md-1">
                                <div class="form-group">
                                    <label>DNS Name</label>
                                    <input id="dnsname" type="text" class="form-control" placeholder="mysubdomainname (use @ for root)">
                                </div>
                            </div>
    
                            <div class="col-md-3 px-md-1">
                                <div class="form-group">
                                    <label>Server IP</label>
                                    <input id="sip" type="text" class="form-control" placeholder="192.168.0.1">
                                </div>
                            </div>
    
                            <div class="col-md-3 px-md-1">
                                <div class="form-group">
                                    <label>Server Port</label>
                                    <input id="sp" type="number" class="form-control" placeholder="80">
                                </div>
                            </div>

                            <div class="col-md-3 px-md-1"><br>
                                <button id="addBtn" class="btn btn-fill btn-primary" onclick="Add()">Add</button>
                            </div>
                            
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                    <table class="table tablesorter " id="">
                        <thead class=" text-primary">
                        <tr>
                            <th>
                                Domain
                            </th>
                            <th>
                                IP
                            </th>
                            <th width="33%">
                                Options
                            </th>
                        </tr>
                        </thead>
                        <tbody id="tbody">
                        <% let i = 0; domain.dns.forEach(dns => { %>
                            <tr>
                                <td>
                                    <%= dns.name %>.<%= domain.name %>
                                </td>
                                <td>
                                    <%= dns.ip %>:<%= dns.port %>
                                </td>
                                <td>
                                    <button class="btn btn-fill btn-primary" onclick="openEdit(<%= i %>)">Edit</button> 

                                    <button class="btn btn-fill btn-primary" onclick="Delete(<%= i %>)">Delete</button>
                                </td>
                            </tr>
                        <% i++ }) %>
                        </tbody>
                    </table>
                    </div>
                </div>
                </div>
            </div>
            </div>
        </div>
        <footer class="footer">
            <div class="container-fluid">
            <ul class="nav">
                
            </ul>
            <div class="copyright">
                © Phaze#6193
                <script>document.write(new Date().getFullYear())</script>
            </div>
            </div>
        </footer>
        </div>
    </div>
    <div class="fixed-plugin">
        <div class="dropdown show-dropdown">
        <a href="#" data-toggle="dropdown">
            <i class="fa fa-cog fa-2x"> </i>
        </a>
        <ul class="dropdown-menu">
            <li class="header-title"> Sidebar Background</li>
            <li class="adjustments-line">
            <a href="javascript:void(0)" class="switch-trigger background-color">
                <div class="badge-colors text-center">
                    <span onclick="document.cookie = 'theme=pink';" class="badge filter badge-primary active" data-color="primary"></span>
                    <span onclick="document.cookie = 'theme=blue';" class="badge filter badge-info" data-color="blue"></span>
                    <span onclick="document.cookie = 'theme=green';" class="badge filter badge-success" data-color="green"></span>
                </div>
                <div class="clearfix"></div>
            </a>
            </li>
            <li class="adjustments-line text-center color-change">
                <span class="color-label">LIGHT MODE</span>
                <span class="badge light-badge mr-2" onclick="document.cookie = 'dark=1';"></span>
                <span class="badge dark-badge ml-2" onclick="document.cookie = 'dark=0';"></span>
                <span class="color-label">DARK MODE</span>
            </li>
            </li>
        </ul>
        </div>
    </div>
    <!--   Core JS Files   -->
    <script src="../assets/js/core/jquery.min.js"></script>
    <script src="../assets/js/core/popper.min.js"></script>
    <script src="../assets/js/core/bootstrap.min.js"></script>
    <script src="../assets/js/plugins/perfect-scrollbar.jquery.min.js"></script>
    <!--  Google Maps Plugin    -->
    <!-- Place this tag in your head or just before your close body tag. -->
    <!-- Chart JS -->
    <script src="../assets/js/plugins/chartjs.min.js"></script>
    <!--  Notifications Plugin    -->
    <script src="../assets/js/plugins/bootstrap-notify.js"></script>
    <!-- Control Center for Black Dashboard: parallax effects, scripts for the example pages etc -->
    <script src="../assets/js/black-dashboard.min.js?v=1.0.0"></script><!-- Black Dashboard DEMO methods, don't include it in your project! -->
    <script src="../assets/demo/demo.js"></script>
    <script>
        let dnsBox = document.getElementById('dnsBoxThingy');
        dnsBox.style.opacity = '0';
        dnsBox.style.height = '0px'
        dnsBox.style.transition = '0.25s'

        let text = '<% domain.dns.forEach(dns => { %><tr><td><%= dns.name %>.<%= domain.name %></td><td><%= dns.ip %>:<%= dns.port %></td><td><button class="btn btn-fill btn-primary" onclick="">Edit</button> <button class="btn btn-fill btn-primary" onclick="">Delete</button></td></tr><% }) %>'

        let dns = JSON.parse(`<%- JSON.stringify(domain.dns) %>`)

        let openDnsAdder = () => {
            dnsBox.style.opacity = '1';
            dnsBox.style.height = '100px';
        }

        let Add = () => {
            let dnsname = document.getElementById('dnsname').value;
            let sip = document.getElementById('sip').value;
            let sp = document.getElementById('sp').value;
            let addBtn = document.getElementById('addBtn');

            addBtn.innerHTML = 'Add'

            if(editOpen === true)return edit();

            if(dns.find(x => x.name === dnsname))return demo.showNotification('bottom','left', 'That record already exists', '', 'warning');

            dnsBox.style.opacity = '0';
            dnsBox.style.height = '0px'

            let tbody = document.getElementById('tbody');
            let newText = ''

            dns.push({
                name: dnsname,
                ip: sip,
                port: sp
            })

            let index = 0
            dns.forEach(dns => {
                newText = newText + '<tr><td>'+dns.name+'.<%= domain.name %></td><td>'+dns.ip+':'+dns.port+'</td><td><button class="btn btn-fill btn-primary" onclick="openEdit('+index+')">Edit</button> <button class="btn btn-fill btn-primary" onclick="Delete('+index+')">Delete</button></td></tr>'
                index++
            })

            tbody.innerHTML = newText

            console.log(dns)

            save()

            document.getElementById('dnsname').value = ''
            document.getElementById('sip').value = ''
            document.getElementById('sp').value = ''
        }

        let openEdit = index => {
            dnsBox.style.opacity = '1';
            dnsBox.style.height = '100px';

            console.log('rfgefrfghvkljgh')
            editOpen = true;
            editIndex = index;

            let dnsname = document.getElementById('dnsname');
            let sip = document.getElementById('sip');
            let sp = document.getElementById('sp');
            let addBtn = document.getElementById('addBtn');

            addBtn.innerHTML = 'Edit'

            let dnsRecord = dns[index];

            dnsname.value = dnsRecord.name;
            sip.value = dnsRecord.ip;
            sp.value = dnsRecord.port;

            save()
        }

        let Delete = index => {
            let record = dns[index]

            dns = dns.filter(x => x.name !== record.name);

            let tbody = document.getElementById('tbody');
            let newText = ''

            let index2 = 0
            dns.forEach(dns => {
                newText = newText + '<tr><td>'+dns.name+'.<%= domain.name %></td><td>'+dns.ip+':'+dns.port+'</td><td><button class="btn btn-fill btn-primary" onclick="openEdit('+index2+')">Edit</button> <button class="btn btn-fill btn-primary" onclick="Delete('+index2+')">Delete</button></td></tr>'
                index2++
            })

            tbody.innerHTML = newText

            save()
        }

        let edit = () => {
            let dnsname = document.getElementById('dnsname').value;
            let sip = document.getElementById('sip').value;
            let sp = document.getElementById('sp').value;

            dns[editIndex] = {
                name: dnsname,
                ip: sip,
                port: sp
            }

            let tbody = document.getElementById('tbody');
            let newText = ''

            let index = 0
            dns.forEach(dns => {
                newText = newText + '<tr><td>'+dns.name+'.<%= domain.name %></td><td>'+dns.ip+':'+dns.port+'</td><td><button class="btn btn-fill btn-primary" onclick="openEdit('+index+')">Edit</button> <button class="btn btn-fill btn-primary" onclick="Delete('+index+')">Delete</button></td></tr>'
                index++
            })

            tbody.innerHTML = newText

            save()

            document.getElementById('dnsname').value = ''
            document.getElementById('sip').value = ''
            document.getElementById('sp').value = ''

            dnsBox.style.opacity = '0';
            dnsBox.style.height = '0px'
        }

        let save = () => {
            console.log(dns)

            fetch('/api/v1/domains/<%= domain.name %>/dns/edit', {
                method: 'POST',
                headers: {
                    savedata: JSON.stringify(dns)
                }
            }).then(data => data.text()).then(data => {
                if(data === "ok"){
                    demo.showNotification('bottom','left', 'Successfully saved DNS ', '', 'success');
                } else{
                    demo.showNotification('bottom','left', 'ERROR: Couldn\'t save DNS<br>Response Code: '+data, '', 'danger')
                } 
            })
        }

        let notification = document.getElementById('notifications');
        let alertDot = document.getElementById('alertDot');
        let notifications = []

        fetch('/api/v1/alerts').then(data => data.json()).then(data => {
            let text = ''
            notifications = []

            data.forEach(alert => {
                text = text + '<li class="nav-link"><a href="'+alert.href+'" class="nav-item dropdown-item">'+alert.message+'</a></li>'
                notifications.push(alert);
            })

            notification.innerHTML = text
            if(notifications.length > 0){
                alertDot.style.opacity = '1';
            } else{
                alertDot.style.opacity = '0';
            }
        })

        setInterval(() => {
            fetch('/api/v1/alerts').then(data => data.json()).then(data => {
                let text = ''
                notifications = []

                data.forEach(alert => {
                    text = text + '<li class="nav-link"><a href="'+alert.href+'" class="nav-item dropdown-item">'+alert.message+'</a></li>'
                    notifications.push(alert);
                })

                notification.innerHTML = text
                if(notifications.length > 0){
                    alertDot.style.opacity = '1';
                } else{
                    alertDot.style.opacity = '0';
                }
            })
        }, 1000)
    </script>
    <script>
        $(document).ready(function() {
        $().ready(function() {
            $sidebar = $('.sidebar');
            $navbar = $('.navbar');
            $main_panel = $('.main-panel');

            $full_page = $('.full-page');

            $sidebar_responsive = $('body > .navbar-collapse');
            sidebar_mini_active = true;
            white_color = false;

            window_width = $(window).width();

            fixed_plugin_open = $('.sidebar .sidebar-wrapper .nav li.active a p').html();



            $('.fixed-plugin a').click(function(event) {
            if ($(this).hasClass('switch-trigger')) {
                if (event.stopPropagation) {
                event.stopPropagation();
                } else if (window.event) {
                window.event.cancelBubble = true;
                }
            }
            });

            $('.fixed-plugin .background-color span').click(function() {
            $(this).siblings().removeClass('active');
            $(this).addClass('active');

            var new_color = $(this).data('color');

            if ($sidebar.length != 0) {
                $sidebar.attr('data', new_color);
            }

            if ($main_panel.length != 0) {
                $main_panel.attr('data', new_color);
            }

            if ($full_page.length != 0) {
                $full_page.attr('filter-color', new_color);
            }

            if ($sidebar_responsive.length != 0) {
                $sidebar_responsive.attr('data', new_color);
            }
            });

            $('.switch-sidebar-mini input').on("switchChange.bootstrapSwitch", function() {
            var $btn = $(this);

            if (sidebar_mini_active == true) {
                $('body').removeClass('sidebar-mini');
                sidebar_mini_active = false;
                blackDashboard.showSidebarMessage('Sidebar mini deactivated...');
            } else {
                $('body').addClass('sidebar-mini');
                sidebar_mini_active = true;
                blackDashboard.showSidebarMessage('Sidebar mini activated...');
            }

            // we simulate the window Resize so the charts will get updated in realtime.
            var simulateWindowResize = setInterval(function() {
                window.dispatchEvent(new Event('resize'));
            }, 180);

            // we stop the simulation of Window Resize after the animations are completed
            setTimeout(function() {
                clearInterval(simulateWindowResize);
            }, 1000);
            });

            $('.switch-change-color input').on("switchChange.bootstrapSwitch", function() {
            var $btn = $(this);

            if (white_color == true) {

                $('body').addClass('change-background');
                setTimeout(function() {
                $('body').removeClass('change-background');
                $('body').removeClass('white-content');
                }, 900);
                white_color = false;
            } else {

                $('body').addClass('change-background');
                setTimeout(function() {
                $('body').removeClass('change-background');
                $('body').addClass('white-content');
                }, 900);

                white_color = true;
            }


            });

            $('.light-badge').click(function() {
            $('body').addClass('white-content');
            });

            $('.dark-badge').click(function() {
            $('body').removeClass('white-content');
            });
        });
        });
    </script>
    <script>
        $(document).ready(function() {
        // Javascript method's body can be found in assets/js/demos.js
        demo.initDashboardPageCharts();

        });
    </script>

    </body>

</html>